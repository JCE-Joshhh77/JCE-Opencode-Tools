# Project Context
<!-- session: 2026-06-27T00:39:21.437Z | count: 506 | last-prune: 2026-06-27 | last-update: 2026-06-27T00:53:38.362Z | content-hash: 3ee73bdb | stale-sessions: 0 -->
> Auto-maintained by AI. You can edit this file freely.
> Archived entries: see .opencode-context-archive.md
> Last updated: 2026-06-27

## Stack
- TypeScript + Bun (runtime & test)
- Commander.js (CLI framework)
- Chalk (terminal colors)
- AJV (JSON schema validation)
- @modelcontextprotocol/sdk + Zod (MCP server)
- PowerShell + Bash (installers)

## Architecture Decisions
- JCE hardening implemented: evidence auto-capture, closed-loop orchestration blocker, skill harden CLI, agent audit, canonical docs, resolver v2, capability roadmap, analytics recommendations.
- JCE context index will keep `.opencode-context.md` compact and store detailed buckets/notes under `.opencode-jce/context/`.
- Frontend agent should be upgraded in-place, not added, preserving 42-agent config invariant while adding non-AI visual craft and backend contract mapping behavior.

## Conventions
- Commit format: `<type>(<scope>): <description>`
- Version must stay in sync across: package.json, install.ps1, install.sh, constants.ts, version.ts
- Skills written in Bahasa Indonesia

## Current Status
  -st at install time instead of maintaining independent hardcoded runtime-critical file lists.
- Released `v3.7.2`: commit `aab9fe1` pushed to `main`, tag `v3.7.2` pushed, GitHub release created at `https://github.com/JCE-Joshhh77/JCE-Opencode-Tools/releases/tag/v3.7.2`. Scope covers singl
  -e-source install/update payload manifest hardening plus version/changelog sync; local-only leftovers remain `.github/workflows/ci.yml`, `.opencode-context*.md`, and `plan.md`.
- Fixed post-release updater bug from `3.7.2`: installed self-update now resolves payload manifest from either `<base>/config/cli-payload.txt` or `<base>/cli/config/cli-payload.txt`, so second-stage
  update handoff no longer fails on installed CLI layout.
- Released `v3.7.3`: commit `81f3c1a` pushed to `main`, tag `v3.7.3` pushed, GitHub release created at `https://github.com/JCE-Joshhh77/JCE-Opencode-Tools/releases/tag/v3.7.3`. Scope fixes ins
  -talled self-update manifest resolution after `3.7.2`; local-only leftovers remain `.github/workflows/ci.yml`, `.opencode-context*.md`, and `plan.md`.
- Compacted 9 older current status entries into archive/context history.
- Released `v3.8.1`: token savings honesty + expanded context budget + skill dedup. Tag pushed, release created.
- Patched over-confirmation guardrails so normal reversible work proceeds on safest reasonable assumption instead of repeated continue/confirm pauses; updated global AGENTS, jce-worker prompt, orches­
tration blocker classifier, and prompt/orchestration tests.
- Reduced over-confirmation behavior by removing unconditional global confirm step, tightening ambiguity rules to allow safe assumptions, broadening user-approved scope to include necessary implicit s
- Last touched: config/AGENTS.md, src/plugin/agents/jce-worker.ts, config/skills/estimation-planning/SKILL.md, config/skills/orchestration-patterns/SKILL.md, src/plugin/lib/orchestration/blocker-classifier.ts, tests/unit/orchestration-intelligence-upgrades.test.ts.
- Verified: bun test tests/unit/plugin-agents.test.ts tests/unit/orchestration-intelligence-upgrades.test.ts -> 31 pass, 0 fail.
- Next: Smoke-test actual jce-worker behavior with gpt-5.4 on representative multi-step prompts to confirm fewer unnecessary 'continue?' pauses..
- Next: If behavior still too cautious, inspect upstream OpenCode orchestration or model-side settings outside repo prompts..
- Prepared `v3.8.2` release for over-confirmation reduction: prompt/runtime alignment, blocker classifier safe-assumption retry, version/changelog sync, focused verification green (`bun run typecheck`
- `bun test tests/unit/plugin-agents.test.ts tests/unit/orchestration-intelligence-upgrades.test.ts tests/unit/ui.test.ts tests/unit/plugin-workflow-tool.test.ts` -> 55 pass, 0 fail).
- Released `v3.8.2`: commits `3424ca0` (plus prior release commit `d799c7c`) pushed to `main`, tag `v3.8.2` points to `3424ca0`, GitHub release created at `https://github.com/JCE-Joshhh77/JCE-Opencode
-Tools/releases/tag/v3.8.2`. Scope: reduced over-confirmation friction, prompt/runtime alignment, and version/changelog sync; local-only leftovers remain `.github/workflows/ci.yml`, `.opencode-context
-archive.md`, `.opencode-context.md`, and `plan.md`.
- Added generated/build artifact guardrail: `tool-discipline.ts` now flags paths like `public/assets/*`, `dist/*`, `build/*`, minified/hash assets as brittle for line-based edits, and `workflow-assi
-stant.ts` now surfaces a Generated Artifact Guardrail in `code_task_plan` advising source-file edits or exact string replacement after fresh read. Verified with `bun test tests/unit/plugin-tool-disc
-ipline.test.ts tests/unit/plugin-workflow-tool.test.ts` and `bun run typecheck`.
- Fixed `doctor --fix` npm-based LSP auto-install path for Ubuntu/Linux without sudo: `fixer.ts` now retries failed `npm install -g` with user prefix `~/.opencode-jce/npm-global`, prepends its `bin` 
-to current PATH so follow-up merge sees installed servers, and reports clearer failure causes for `EACCES` and engine mismatches. Verified with `bun test tests/unit/audit-fixes.test.ts` (55 pass) and
- `bun run typecheck`.
- Prepared `v3.8.3` release: Ubuntu/Linux `doctor --fix` now falls back from failing global npm LSP installs to `~/.opencode-jce/npm-global`, plus generated-asset line-edit guardrails and version/
-changelog sync. Focused verification green: `bun test tests/unit/audit-fixes.test.ts tests/unit/plugin-tool-discipline.test.ts tests/unit/plugin-workflow-tool.test.ts tests/unit/ui.test.ts` -> 8
-4 pass, 0 fail; `bun run typecheck` exit 0.
- Released `v3.8.3`: commit `da742af` pushed to `main`, tag `v3.8.3` pushed, GitHub release created at `https://github.com/JCE-Joshhh77/JCE-Opencode-Tools/releases/tag/v3.8.3`. Scope: Ubuntu/Li
-nux doctor npm-LSP fallback, clearer install failure causes, generated-asset edit guardrails, and version/changelog sync; local-only leftovers remain `.github/workflows/ci.yml`, `.opencode-context*
-.md`, and `plan.md`.
- Expanded `doctor --fix` LSP auto-fix beyond npm-only: `fixer.ts` now classifies/installers for Rust, Go, Java/jdtls, clangd, csharp-ls, Kotlin LS, Dart SDK, Lua LS, Marksman, Taplo, Solargraph
-, Elixir LS, and Metals across Ubuntu/Linux and Windows using safe package-manager or user-space bootstrap paths where possible.
- Verified with `bun test tests/unit/audit-fixes.test.ts` (56 pass) and `bun run typecheck`.
- Prepared `v3.8.4` release: `doctor --fix` now auto-classifies and auto-installs broader LSP set across Ubuntu/Linux and Windows (Rust/Go/Java/clangd/csharp/Kotlin/Dart/Lua/Marksman/Taplo/Solargraph
-/Elixir/Metals) instead of npm-only/manual handling. Focused verification green: `bun test tests/unit/audit-fixes.test.ts tests/unit/plugin-workflow-tool.test.ts tests/unit/ui.test.ts` -> 81 p
-ass, 0 fail; `bun run typecheck` exit 0.
- Prepared `v3.8.5` release: direct `read` output no longer goes through aggressive post-tool context-budget compression, reducing worker termination risk on low-memory VPS setups. Focused verificat
-ion green: `bun test tests/unit/plugin-integration.test.ts tests/unit/plugin-workflow-tool.test.ts tests/unit/ui.test.ts` -> 80 pass, 0 fail; `bun run typecheck` exit 0.
Security audit (s483): 0 CRIT, 2 HIGH, 4 MED, 4 LOW.
Top: curl|bash no checksum (install.sh:263), tarball no integrity (install.sh:311), 56 JSON.parse no AJV.
Safe: update.ts SHA TOCTOU, team.ts https validation, bucket sanitized, traversal blocked.
Audit s485 install/update/uninstall flow: 0 CRIT, 3 HIGH, 6 MED, 5 LOW.
Top HIGH: curl|bash no checksum (install.sh:263,670), atomic rename EBUSY on Windows (update.ts:416-436), uninstall orphans shim (uninstall.ts:509-538).
MED: postinstall.ts:40 writeFileSync unwrapped, README badge 3.8.0 drift, npm bin -g deprecated (install.sh:465).
Version sync 3.8.5 OK across package.json/install.ps1/install.sh/constants.ts/version.ts/CHANGELOG.md; README badge drifts at 3.8.0.
TODO(decompose) in update.ts:1, controller.ts:4, plugin/index.ts:1 - refactor backlog, low priority.
- Added Mandatory Root Cause Gate to global AGENTS and jce-worker prompt: error reports now require exact error/log or repro evidence, failure classification, Root Cause Evidence, minimal fix, and rer
-un verification; speculative fixes and broad refactors during bugfix are forbidden.
- Verified Root Cause Gate change with `bun test tests/unit/plugin-agents.test.ts` (24 pass, 0 fail) and `bun run typecheck` (exit 0).
- Released `v3.8.6`: commit `ae2a76a` pushed to `main`, tag `v3.8.6` pushed, GitHub release created at `https://github.com/JCE-Joshhh77/JCE-Opencode
-Tools/releases/tag/v3.8.6`. Scope: Mandatory Root Cause Gate in global AGENTS + jce-worker, version/changelog sync, and CLI payload now includes agent prompt files so updated users receive same
- behavior. Verification: `bun test` -> 1299 pass, 0 fail; `bun run typecheck` exit 0; `bun ./src/index.ts validate` OK; Git Bash `bash -n install.sh` OK; CLI version 3.8.6.
- Audit s489 whole plugin: no edits. Verification: `bun run typecheck` exit 0; `bun test` 1297 pass/2 fail/1 error under full suite due timeout/flaky, focused `bun test tests/integration/cli.test.ts` 
- 7 pass and `bun test tests/unit/config.test.ts` 33 pass; `bun ./src/index.ts validate` OK; `bun audit` found 6 advisories (hono high/moderate, @babel/core low).
- Audit findings: 0 CRIT; HIGH supply-chain gaps in install.sh curl|bash/tarball, install.ps1 JDTLS snapshot, fixer.ts latest release downloads, dependency advisories; MED plugin MCP trust boundary,
 context cross-project sibling read, Windows update rename reliability, uninstall stale shims, unvalidated JSON config; LOW full-suite flake/timeout, hardcoded subagent model routing.
- Explorer subagent audit failed due provider account; code-quality audit completed manually in main cx/gpt-5.5.
- Fixed audit issues s489/s490: dependency audit clean via hono/@babel overrides; unverified Bun/rustup/JDTLS/fixer downloads now blocked by default unless explicit OPENCODE_JCE_ALLOW_UNVERIFIED_* env
; plugin local MCP requires `--yes --allow-local-mcp`; related project context default scoped to project root; update dir swaps retry/rollback; uninstall removes stale shims; team/plugin/config JSON 
trust boundaries validate shape; subagent category model map no longer hardcodes Anthropic; slow tests have explicit timeouts.
- Verification: `bun run typecheck` exit 0; `bun audit` no vulnerabilities; focused 170 tests pass; `bun ./src/index.ts validate` OK; `bun test` 1299 pass 0 fail; Git Bash `bash -n install.sh` OK.
- Residual: `bash` not on PATH in PowerShell, verified via `C:\Program Files\Git\bin\bash.exe`; unverified download env overrides remain intentional escape hatches for manual opt-in.
- Added per-agent model config for all configured agents: `jce-plugin.json` now accepts arbitrary safe agent IDs; `plugin configure/models` lists native JCE + `agents.json`; `loadAgents()` applies 
valid `model` overrides to legacy agents; native plugin agents still default to active OpenCode model unless override valid.
- Self-audit found and fixed two regressions: removed accidental direct `hono` dependency, and made `agents.json` model override runtime-effective by adding optional schema/type support and load-time 
application.
- Verification after self-audit fixes: `bun run typecheck` exit 0; `bun audit` no vulnerabilities; focused agent/schema/CLI tests 95 pass; `bun ./src/index.ts validate` OK; full `bun test` 
1301 pass, 0 fail.
- Added OpenCode slash-command support for per-agent model settings via `command.execute.before`: `/jce-models` lists agents/models; `/jce-agent-model <agent> <provider/model|default>` writes 
`jce-plugin.json` using same validation as CLI. This works inside OpenCode command UI when plugin command hook is active.
- Verification: `bun run typecheck` exit 0; `bun audit` no vulnerabilities; slash/model focused tests 65 pass; `bun ./src/index.ts validate` OK; full `bun test` 1302 pass, 0 fail.
- Fixed per-agent slash model runtime activation: `/jce-agent-model` now updates native plugin agent config in memory as well as `jce-plugin.json`, so no OpenCode restart needed for native JCE agents.
- Added regression test `slash command updates native agent config without plugin restart` in `tests/unit/plugin-integration.test.ts`.
- Verification: focused `bun test tests/unit/plugin-integration.test.ts tests/unit/plugin-settings.test.ts` -> 66 pass, 0 fail; `bun run typecheck` exit 0; full chain `bun audit && bun ./src/index.tsx
 validate && bun test` progressed through full tests -> 1303 pass, 0 fail.
- Correction: validation command was `bun ./src/index.ts validate` (not `index.tsx`).
- Removed uncertainty for custom `agents.json` slash model activation: `/jce-agent-model` now calls OpenCode SDK `config.get/update` and injects/updates live `config.agent[agent]` for both native JCE+
 and custom agents, while still persisting `jce-plugin.json`.
- Added regression test `slash command updates custom agents.json agent through live OpenCode config`; proves custom agent gets prompt/tools/model live and `default` removes model live.
- Verification: `bun test tests/unit/plugin-integration.test.ts tests/unit/plugin-settings.test.ts` -> 67 pass, 0 fail; `bun run typecheck` exit 0; `bun ./src/index.ts validate` OK.
- Fixed agent reachability gap: plugin `config` hook now injects every valid `agents.json` custom agent into OpenCode `config.agent` (prompt/tools/mode/model) without overwriting existing user agents.
- Fixed dispatch reachability gap: `dispatch` tool now accepts any known agent ID from native JCE + `agents.json`, validates unknown IDs, and can launch custom agents instead of hardcoded 5-agent enum
- Added tests for all callable paths: config injects every custom agent; dispatch launches custom `agents.json` agent; slash command updates custom live config. Full `bun test` -> 1306 pass, 0 fail.
- Released `v3.8.7`: commit `14f2385` pushed to `main`, tag `v3.8.7` pushed, GitHub release created at `https://github.com/JCE-Joshhh77/JCE-Opencode-Tools/releases/tag/v3.8.7`.
- Release scope: per-agent OpenCode slash model controls, live custom agent activation/reachability, dispatch custom-agent support, audit hardening for downloads/MCP/context/update/uninstall/JSON, and
 version/changelog sync.
- Verification for `v3.8.7`: `bun run typecheck` pass; `bun audit` no vulnerabilities; `bun ./src/index.ts validate` OK; `bun test` 1306 pass, 0 fail; Git Bash `bash -n install.sh` OK; CLI version 3.8
.7.
- Post-release correction after user noticed too many OpenCode agents: root cause was custom `agents.json` agents being injected into `config.agent` and dispatch accepting custom IDs. Fixed locally by
 restricting `/jce-models` + `/jce-agent-model` + config injection back to native JCE plugin agents only (`jce-worker`, `oracle`, `jce-researcher`, `explorer`, `frontend`, `android`) and dispatch
 back to native subagents only.
- Verification for local correction: `bun test tests/unit/plugin-integration.test.ts tests/unit/plugin-settings.test.ts tests/unit/plugin-tools.test.ts` -> 87 pass, 0 fail; `bun run typecheck` exit 0.
- Released `v3.8.8`: commit `959ddcd` pushed to `main`, tag `v3.8.8` pushed, GitHub release created at `https://github.com/JCE-Joshhh77/JCE-Opencode-Tools/releases/tag/v3.8.8`.
- Scope: fixes `v3.8.7` agent-list regression by limiting OpenCode registration/slash model config to native JCE plugin agents only and restricting dispatch back to intended native subagents;
 version/changelog sync to 3.8.8.
- Verification: `bun run typecheck` pass; `bun audit` no vulnerabilities; `bun ./src/index.ts validate` OK; `bun test` 1305 pass, 0 fail; Git Bash `bash -n install.sh` OK; CLI version 3.8.8.
- Fixed `/jce-models` slash command discoverability: TUI plugin now registers palette slash commands `jce-models` and `jce-agent-model`; server hook still handles execution.
- Verification: `bun test tests/unit/plugin-entry.test.ts` -> 8 pass, 0 fail; `bun run typecheck` exit 0; prior focused `plugin-integration` + `plugin-settings` -> 68 pass, 0 fail.
- Re-released `v3.8.8` after slash-command discoverability fix: commit `f0f0ddc` pushed to `main`, tag `v3.8.8` force-moved/recreated, GitHub release recreated at `https://github.com/JCE-Joshhh77/JCE-
Opencode-Tools/releases/tag/v3.8.8`. Scope adds TUI palette registration for `/jce-models` and `/jce-agent-model` plus changelog update.
- Verification before re-release: `bun run typecheck` pass; `bun audit` no vulnerabilities; `bun ./src/index.ts validate` OK; `bun test` 1306 pass, 0 fail; Git Bash `bash -n install.sh` OK; CLI `3.8.8
- Corrected `v3.8.8` re-release after user screenshot showed palette-selected `/jce-models` only displayed placeholder toast. Root cause: TUI command `run()` did not execute server slash hook; it now
renders `JCE Agent Models` dialog via shared settings/model listing helper. Commit `53e1cae` pushed; tag/release `v3.8.8` recreated at same URL.
- Verification: `bun run typecheck` pass; `bun audit` no vulnerabilities; `bun ./src/index.ts validate` OK; `bun test` 1307 pass, 0 fail; Git Bash `bash -n install.sh` OK; CLI `3.8.8`.
- Updated `/jce-models` UI again after user requested all models without huge static panel: TUI now uses `DialogSelect` scrollable/filterable list with disabled agent rows and model rows showing exact
`/jce-agent-model <agent> <model>` usage. Commit `58ef932` pushed; tag/release `v3.8.8` recreated at same URL.
- Verification: full release chain before final trim (`typecheck`, `audit`, `validate`, `bun test` 1307 pass, `bash -n install.sh`, CLI `3.8.8`); final focused recheck `plugin-entry` 9 pass and `type
check` pass after removing unused helper.
- Investigated Factory Droid feasibility: current plugin is OpenCode-bound at entry/config/tool/session/slash/TUI layers; agents/prompts and Android analysis libs are portable. No Factory Droid API/re
-ferences found in repo, so exact support needs Factory Droid plugin/tool/agent contract before implementation.
- Factory/Droid research: Factory supports plugins with `.factory-plugin/plugin.json`, `skills/`, `droids/`, `commands/`, `mcp.json`, `hooks/`; Droid custom droids map well to JCE native agents, and J
-CE tools should be exposed via MCP/SDK-backed MCP rather than OpenCode hooks.
- Added Factory Droid support without removing OpenCode path: `opencode-jce factory export` generates `.factory-plugin/plugin.json`, `droids/`, `skills/`, `commands/`, `mcp.json`, and installer
-s now auto-export `${CONFIG_DIR}/factory-jce` after CLI install. Verified focused Factory tests, typecheck, validate, shell/PowerShell installer syntax.
- Factory Droid self-audit found/fixed issues before handoff: droid tool frontmatter no longer `read-only` for editing agents; CLI/MCP path now explicit via `cliDir`; README/install command uses ac
-tual marketplace basename; PowerShell export now checks `$LASTEXITCODE`. Verification stayed green.
- Released `v3.8.9`: commit `d3da487` pushed to `main`, tag `v3.8.9` pushed, GitHub release created at `https://github.com/JCE-Joshhh77/
JCE-Opencode-Tools/releases/tag/v3.8.9`. Scope: Factory Droid export/package support from same installer plus version/changelog sync. Verification: typecheck, audit, validate, full tests 1308 pass.
- Post-3.8.9 local fix: install/update Factory Droid flow now cancels Droid plugin install when `droid` CLI is missing and prints install instructions (`curl/irm/npm`) instead of silently treating it
- as later manual setup. Not committed/released yet.
- Released `v3.8.10`: commit `dc4298d` pushed to `main`, tag `v3.8.10` pushed, GitHub release created at `https://github.com/JCE-Joshhh77
/JCE-Opencode-Tools/releases/tag/v3.8.10`. Scope: update exports Factory Droid package, prompts optional Droid install, and cancels with install instructions when `droid` missing. Full tests 1309 pass
- Local post-3.8.10 fix for update fetch failures: update merge now uses freshly downloaded installed CLI config (`configDir/cli/config`) as primary source for agents/mcp/lsp/profiles/prompts/
AGENTS/fallback before GitHub API/raw fallback, preventing `2/8 fetch(es) failed` rollback after CLI self-update. Not committed/released yet.
- Released `v3.8.11`: commit `c341f76` pushed to `main`, tag `v3.8.11` pushed, GitHub release created at `https://github.com/JCE-Joshhh77
/JCE-Opencode-Tools/releases/tag/v3.8.11`. Scope: update config merge now uses local CLI config source before GitHub fallback, preventing `2/8 fetch(es) failed` rollback.
- Local post-3.8.11 fix for Factory Droid marketplace error: `factory-jce` export now creates valid marketplace root with `.factory-plugin/marketplace.json` and plugin under `factory-jce/jce
-opencode-tools/`; generated layout verified and tests/typecheck pass. Not committed/released yet.
- Released `v3.8.12`: commit `198b2fe` pushed to `main`, tag `v3.8.12` pushed, GitHub release created at `https://github.com/JCE-Joshhh77/JCE-Opencode-Tools/releases/tag/v3.8.12`.
- Scope: Factory Droid export now creates valid marketplace root `factory-jce/.factory-plugin/marketplace.json` and plugin under `factory-jce/jce-opencode-tools/`, fixing Droid marketplace add/install
- Verification reused from release prep: `bun run typecheck` exit 0; `bun audit` no vulnerabilities; `bun ./src/index.ts validate` exit 0; Git Bash `bash -n install.sh` exit 0; PowerShell parser OK; `
- Local-only leftovers remain `.github/workflows/ci.yml`, `.opencode-context*.md`, and `plan.md`.
- User verified `v3.8.12` update/install path on Windows: `opencode-jce update` upgraded `3.8.11 -> 3.8.12`, exported Factory Droid marketplace to `%USERPROFILE%/.config/opencode/factory-jce`, `droidÂ
- User verified Droid marketplace commands succeeded: `droid plugin marketplace add C:\Users\Joshhh\.config\opencode\factory-jce` and `droid plugin install jce-opencode-tools@factory-jce` both success
- Local post-3.8.12 fix for Factory Droid usability: `factory export --sync-personal` now writes `~/.factory/AGENTS.md`, droids, skills, and `~/.factory/mcp.json`, and removes `${PROJECT_ROOT}` env.
- User environment synced live with local fix: `bun ./src/index.ts factory export --output %USERPROFILE%/.config/opencode/factory-jce --clean --sync-personal`; Droid plugin updated to cache `mqt5ok6`.
- Verification: `bun test tests/unit/factory-droid.test.ts` 2 pass; `bun run typecheck` exit 0; Git Bash `bash -n install.sh` exit 0; PowerShell parser OK.
- Released `v3.8.13`: commit `547a0f7` pushed to `main`, tag `v3.8.13` pushed, GitHub release created at `https://github.com/JCE-Joshhh77/JCE-Opencode-Tools/releases/tag/v3.8.13`.
- Scope: Factory Droid personal config sync now writes `~/.factory/AGENTS.md`, `~/.factory/droids`, `~/.factory/skills`, and `~/.factory/mcp.json`, and removes `${PROJECT_ROOT}` MCP env placeholder.
- Verification: `bun run typecheck` exit 0; `bun audit` no vulnerabilities; `bun ./src/index.ts validate` OK; focused tests 27 pass; full `bun test` 1310 pass; Git Bash `bash -n install.sh` exit 0; PS
- User retry after update: restart Droid, check header, then `/mcp` retry `context-keeper` if still disconnected.
- Local post-3.8.13 change: Factory Droid install/update now auto-runs `droid plugin marketplace add` and `droid plugin install` when `droid` CLI exists; no prompt/manual commands needed.
- Missing Droid behavior preserved: if `droid` CLI is absent, plugin install is cancelled and platform install instructions are printed.
- Verification: `bun test tests/unit/update-process-cleanup.test.ts tests/unit/factory-droid.test.ts` 8 pass; `bun run typecheck` exit 0; Git Bash `bash -n install.sh` exit 0; PS parser OK.
- Released `v3.8.14`: commit `4a0333f` pushed to `main`, tag `v3.8.14` pushed, GitHub release created at `https://github.com/JCE-Joshhh77/JCE-Opencode-Tools/releases/tag/v3.8.14`.
- Scope: Factory Droid install/update auto-runs `droid plugin marketplace add` and `droid plugin install` when `droid` CLI exists; no prompt/manual commands. Missing Droid CLI still prints install
- Verification: `bun run typecheck` exit 0; `bun audit` no vulnerabilities; `bun ./src/index.ts validate` OK; focused tests 33 pass; full `bun test` 1311 pass; Git Bash `bash -n install.sh`; PS parser
- User retry: `opencode-jce update`, restart Droid, check `Skills`, `MCPs`, `AGENTS.md`, then `/mcp` retry if needed.
- Local post-3.8.14 fix: Factory Droid auto-install now falls back to `droid plugin update jce-opencode-tools@factory-jce` when install says plugin is already installed.
- Live user plugin updated successfully with `droid plugin update jce-opencode-tools@factory-jce` -> `mqt76zv`.
- Released `v3.8.15`: commit `455a17e` pushed to `main`, tag `v3.8.15` pushed, GitHub release created at `https://github.com/JCE-Joshhh77/JCE-Opencode-Tools/releases/tag/v3.8.15`.
- Scope: Factory Droid install/update now falls back to `droid plugin update jce-opencode-tools@factory-jce` when install reports plugin already installed, avoiding manual uninstall/reinstall.
- User retry: `opencode-jce update`; existing `jce-opencode-tools@factory-jce` should update in place.
- Audited Factory provider error: root cause in `C:\Users\Joshhh\.factory\settings.json`, custom models use `provider: generic-chat-completion-api` with `baseUrl: http://localhost:1430/v1`; changing `
provider` to `openai` while keeping localhost baseUrl makes Droid try unreachable local endpoint. User chose to keep all as generic localhost; no repo/config edits made.
- Audited all Droid-related plugin paths: `factory-droid.ts`, `factory.ts`, `update.ts`, installers, tests, generated `~/.factory` files. Verification: `bun test tests/unit/factory-droid.test.ts tests
/unit/update-process-cleanup.test.ts` -> 8 pass; `bun run typecheck` pass; Droid CLI 0.148.0 has `jce-opencode-tools@factory-jce` installed. Findings: generated docs commands unquoted for paths
 with spaces; personal sync overwrites `~/.factory/AGENTS.md`, droids, skills without backup; Droid agents inherit active Factory model so bad BYOK provider config breaks all JCE droids.
- Fixed Droid audit findings: Factory export now quotes marketplace path and sanitizes marketplace names; personal `~/.factory` sync backs up existing `AGENTS.md`, `droids`, and `skills` before overw
rite; factory/update commands surface backup paths and `model: inherit` BYOK warning. Touched `src/lib/factory-droid.ts`, `src/commands/factory.ts`, `src/commands/update.ts`, `tests/unit/factory-droid
test.ts`. Verification: Droid focused tests 10 pass, `bun run typecheck` pass, `bun audit` no vulnerabilities, `bun ./src/index.ts validate` OK.
- Added Factory Droid plugin hooks for context preservation/autocompaction lifecycle: export now writes `hooks/hooks.json` with `PreCompact` (`manual|auto`), `SessionStart`, and `SessionEnd`,
 plus `scripts/jce-context-hook.js` to checkpoint `.opencode-context.md`. Also surfaces hook list in `factory export` output and README. Verification: Droid hook/export tests 11 pass, typecheck pass,
 `bun audit` no vulnerabilities, `bun ./src/index.ts validate` OK, temp export shows hooks JSON.
- Ported OpenCode `/jce-models` and `/jce-agent-model` behavior to Factory Droid export as executable commands. `/jce-models` lists JCE droid models and BYOK custom models; `/jce-agent-model <agent>
 <model|default>` edits `~/.factory/droids/<agent>.md`, auto-prefixes BYOK models with `custom:`, and `syncFactoryDroidPersonalConfig` preserves non-inherit per-droid model settings across updates.
 Verification: Droid focused tests 12 pass, `bun run typecheck` pass, `bun audit` no vulnerabilities, `bun ./src/index.ts validate` OK, temp export includes command scripts.
- Released `v3.8.16`: commit `3cb5838` pushed to `main`, tag `v3.8.16` pushed, GitHub release created at `https://github.com/JCE-Joshhh77/JCE-Opencode
-Tools/releases/tag/v3.8.16`. Scope: Factory Droid context hooks, Droid `/jce-models` + `/jce-agent-model`, personal sync backups, model persistence, quoted/sanitized marketplace paths, version/ch
angelog sync. Verification: focused 37 tests pass, typecheck pass, audit clean, validate OK, installer syntax checks pass. Local-only leftovers unchanged: `.github/workflows/ci.yml`, `.opencode
-context*.md`, `.opencode-context.md.tmp-3892-1781683117822`, `plan.md`.
- Released `v3.8.17`: commit `095a2a0` pushed to `main`, tag `v3.8.17` pushed, GitHub release created at `https://github.com/JCE-Joshhh77/JCE-Opencode
-Tools/releases/tag/v3.8.17`. Scope fixes Droid `/jce-models` and `/jce-agent-model` dumping full JS source by making command files tiny wrappers and moving logic to plugin `scripts/`.
 Verification: focused 37 tests pass, typecheck pass, audit clean, validate OK, installer syntax checks pass. Local-only leftovers unchanged: `.github/workflows/ci.yml`, `.opencode-context*.md`, temp
 context file, `plan.md`.
- Released `v3.8.18`: commit `b3244da` pushed to `main`, tag `v3.8.18` pushed, GitHub release created at `https://github.com/JCE-Joshhh77/JCE-Opencode
-Tools/releases/tag/v3.8.18`. Scope fixes Droid slash command source dumps by exporting `/jce-models` and `/jce-agent-model` as Markdown prompt commands instead of executable command files; backend
 scripts remain under plugin `scripts/`. Verification: focused 37 tests pass, typecheck pass, audit clean, validate OK, installer syntax checks pass. Local-only leftovers unchanged: `.github/workflows
/ci.yml`, `.opencode-context*.md`, temp context file, `plan.md`.
- Added OpenCode TUI picker for `/jce-agent-model`: opens native DialogSelect agent picker, then model/default picker, persists `jce-plugin.json`; `/jce-models` DialogSelect path preserved. Factory/D 
roid remains markdown/script command because no native Droid TUI picker API evidence found.
- Released `v3.8.20`: commit `a278b16` pushed to `main`, tag `v3.8.20` pushed, GitHub release created. Scope: native OpenCode TUI picker for `/jce-agent-model` plus version/changelog sync.
- Verification: `bun test` 1316 pass, `bun run typecheck` pass, `bun audit` clean, `bun ./src/index.ts validate` OK, installer syntax checks OK.
- Checked Factory Droid command path: update/factory export writes `scripts/jce-models.js` and command markdown uses `${DROID_PLUGIN_ROOT}`; if env missing, Droid command expands to `/scripts/...` and
 fails.
- Added CLI `opencode-jce droid models` and `opencode-jce droid agent <agent> <model|default>` to list Factory Droid models and set `~/.factory/droids/<agent>.md` model without using Droid slash env.
- Ensured Droid CLI model command is install/update payload-safe: added `src/commands/droid.ts` to `config/cli-payload.txt`, added payload regression test, and verified droid model list/set smoke path
- Released `v3.8.21`: commit `1579f75` pushed to `main`, tag `v3.8.21` pushed, GitHub release created. Scope: `opencode-jce droid models/agent` CLI model controls and payload coverage.
- Audited orchestration slow-load/hang resilience without edits across context-keeper MCP, plugin hook entry, background spawner/manager, dispatch/workflow/android_logcat tools, orchestration bridge,e
- High findings: context-keeper stdio startup lacks delayed-initialize resilience/handshake and exits hard on connect failure; background session prompts can hang indefinitely until passive stale poll
- Medium findings: plugin hook chain has unguarded awaits in system transform skill loading and translation filter; dispatch/workflow tool calls lack internal timeout wrappers; orchestration node time
- No files changed. Verification limited to read-only code inspection via file reads/grep; no tests/commands run by request.
- Audited orchestration slow-load/hang resilience with no code changes across `src/mcp/context-keeper.ts`, `src/plugin/index.ts`, background manager/spawner, dispatch/workflow/android-logcat tools, `l
- Key evidence: context-keeper already has delayed-initialize warning, EPIPE/stdout resilience, graceful signal handling, and fs timeout wrapper; remaining gaps are unguarded async hook awaits, no top
- Verification for this audit was read-only file inspection via `read`/`grep`; no tests or shell commands run by request.
Audited plugin orchestration; applied 4 slow-OpenCode-load timeout fixes (MCP stdio resilience, spawner session.create/prompt timeouts, fs op timeouts, git status timeout).
MCP context-keeper.ts now swallows EPIPE/SIGPIPE/SIGTERM and stays alive on transient stdio errors during slow OpenCode cold-start.
spawner.ts session.create timeout 60s (env OPENCODE_JCE_BG_SESSION_CREATE_TIMEOUT_MS); session.prompt timeout 12min (env OPENCODE_JCE_BG_PROMPT_TIMEOUT_MS).
Critical bug fixed in withTimeout: do NOT timer.unref() — Bun/Node would exit event loop before rejection fires, hanging the await silently.
Verification: bun run typecheck pass; focused tests 170 pass 0 fail incl 2 new spawner timeout regression tests.
Touched: src/mcp/context-keeper.ts, src/plugin/background/spawner.ts, src/plugin/tools/workflow.ts, tests/unit/plugin-background.test.ts.
Released v3.8.22: commit 552d9ec pushed to main, tag v3.8.22 pushed, release at https://github.com/JCE-Joshhh77/JCE-Opencode-Tools/releases/tag/v3.8.22.
Scope: MCP stdio resilience for slow OpenCode load, spawner session.create/prompt active-fire timeouts (env override), fs/git status 10s timeouts, withTimeout no-unref fix.
Verification: typecheck OK; validate OK; focused 170 tests pass; CLI v3.8.22; bash -n install.sh OK; PS parser OK.
Local leftovers remain unchanged: .github/workflows/ci.yml, .opencode-context*.md, plan.md.
Released v3.8.23: commit 748c749 to main, tag v3.8.23, release https://github.com/JCE-Joshhh77/JCE-Opencode-Tools/releases/tag/v3.8.23.
Scope: 7-fix audit closure: sub-agent Root Cause Gate, shared withTimeout, ID regex, completeTask guard, version-sync test.
Verification: typecheck OK; validate OK; 211 focused tests pass (28 new); CLI v3.8.23; bash -n + PS parser OK.
Audited jce-worker orchestration; found 8 gaps in soft policy enforcement (IntentGate, parallel audit, skill fallback, failure counter, anti-dup, wisdom loop, meta-cog, final contract).
Patched user opencode.json: npx -> bunx + timeout 120000 for github-search/memory/playwright/sequential-thinking; stripped UTF-8 BOM blocking validate.
Added Orchestration Enforcement v4 to jce-worker.ts with 8 explicit hard rules + regression test covering all rules.
Prepared v3.8.24 release: version synced across 9 sites; changelog written.
Verification: typecheck OK; bun test 1343 pass / 0 fail; audit clean; validate OK; install.sh + install.ps1 parsers OK.
Touched: jce-worker.ts, plugin-agents.test.ts, ui.test.ts, plugin-workflow-tool.test.ts, package.json, install.sh/ps1, constants.ts, version.ts, context-keeper.ts, README, CHANGELOG.

## Important Notes
- `v3.6.1` had to be rewritten from annotated tag to lightweight tag because updater clients on `v3.5.5` compare clone HEAD to `refs/tags/<tag>` SHA and cannot resolve tag objects; remote tag now poin
ts directly to commit `99fa373`.
- Audited JCE-Worker runtime vs prompt split: strong verification/delegation guards exist, but prompt-level promises still exceed hard enforcement in auto-skill loading, parallel delegation, and final
