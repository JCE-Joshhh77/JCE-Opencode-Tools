# Context Archive
> Historical decisions and notes. Reference only.

## Archived: 2026-05-05
### Architecture Decisions
- CLI installed to `~/.config/opencode/cli/` via .cmd shim in bun bin
- Config deployed to `~/.config/opencode/` on all platforms, including Windows
- Installer never overwrites existing user config (safe merge)
- Context preservation enforced via MCP server `context-keeper` (src/mcp/context-keeper.ts)
- context-keeper auto-registered to `opencode.json` (NOT mcp.json) via installer + migration
- `config/mcp.json` = Claude Desktop format (legacy); OpenCode reads `opencode.json` key `"mcp"`
- Backup enforcement: TodoWrite completion = context update (IRON RULE in AGENTS.md)
- Auto-Dispatch in AGENTS.md: AI silently selects agent + loads skills per message
- `opencode-jce update` backs up AGENTS.md before overwriting
- LSP auto-detect: installer scans PATH for all lsp.json commands, pre-populates opencode.json
- context-keeper responses include REMINDER lines to enforce AI compliance
- loadOpenCodeConfig() auto-creates full template if file missing (never produces partial config)
- install.ps1 uses [System.IO.File]::WriteAllText with UTF8NoBOM (PS 5.1 compat)
- `opencode-jce update` now also updates ~/.config/opencode/cli/ folder (not just global binary)
- `opencode-jce update` always syncs files (no early exit on same version); --force deprecated

### Important Notes
- Version = "1.9.6" — 8 places to update (incl. test + README badge)
- Migrations always run based on version.json state; initVersionFile uses 1.0.0 base
- @modelcontextprotocol/server-fetch DELETED from npm (May 2026); OpenCode has native webfetch
- MCP defaults now 6 stable servers; filesystem/web-fetch/postgres removed by default and migration/update repair.
- Plugin install now applies manifest mcp config into opencode.json while preserving existing entries.
- Follow-up audit report found additional flow risks; see audit-install-update-features-2026-05-04.txt.
- Verified audit hardening: bun test, typecheck, bash -n install.sh, PowerShell parser all pass.
- Sisyphus plugin verification: `bun test` = 304 pass/0 fail; `bun run typecheck` passed; pushed through commit 7260fce.
- 1.9.6 update fix: existing opencode.json now receives missing default plugin entries during update, preserving existing plugin values.
- Update handoff fix: when version upgrade occurs, old process now restarts `opencode-jce update` with `OPENCODE_JCE_UPDATED_CLI_HANDOFF=1` before config merge.
- Sisyphus model fix: profile resolver validates profile provider/model against opencode.json provider models before using it; otherwise falls back to configured provider models.
- `opencode-jce plugin configure` writes `jce-plugin.json`; null/missing agent model means use active OpenCode model, valid provider/model string pins that agent.


## Archived: 2026-05-06
### Architecture Decisions
- context-keeper v2.0.0: session tracking, enrichment, semantic prune, cross-project, concurrency
- Section utilities extracted to context-sections.ts (prevents circular deps)
- Plugin tools use `tool.schema` (not direct zod import) to match @opencode-ai/plugin's internal zod version
- Sisyphus v2 Phase 1 adds policy/state/contracts plus a verification guard while preserving existing plugin transport.
- Sisyphus v2 Phase 3 adds bounded execution memory, trace events, rich review verdicts, handoff reports, stale detection, and retry metadata.
- Sisyphus v2 Phase 4-6 will use runtime-first hybrid: shared design, separate phase implementation plans.
- Sisyphus v2 Phase 4 Task 1 adds pure workflow runtime model in src/plugin/lib/workflow.ts.
- Sisyphus v2 Phase 4 Task 2 adds pure workflow plan quality validation in src/plugin/lib/plan-quality.ts.
- Plan quality validation uses whole normalized token coverage and rejects overly broad sequenced steps.
- Sisyphus v2 Phase 4 Task 3 adds pure workflow verification gate in src/plugin/lib/verification-gate.ts.
- Sisyphus v2 Phase 4 Task 4 adds pure workflow completion certificate generation in src/plugin/lib/completion-certificate.ts.
- Sisyphus v2 Phase 4 adds WorkflowRun/WorkflowStep, plan quality gate, verification gate, completion certificate, and workflow memory.
- Sisyphus v2 Phase 5 plan keeps decision intelligence as pure helper modules over workflow/runtime memory.
- Sisyphus v2 Phase 5 Task 1 adds pure skill router for intent-to-skill workflow selection.
- Sisyphus v2 Phase 5 Task 2 adds pure execution memory query helpers for evidence, commands, failures, blockers, stale tasks, and retry history.
- Sisyphus v2 Phase 5 Task 3 adds pure typed error taxonomy helper with prioritized category rules.
- Sisyphus v2 Phase 5 Task 4 adds pure recovery decisions and retry prompt builder.
- Recovery blocked handoffs derive completed items from completed workflow steps and use category-specific next options.
- Sisyphus v2 Phase 5 adds skill routing, memory query helpers, typed error taxonomy, and recovery decisions.
- Sisyphus v2 Phase 6 starts CLI-first with status, trace, report, and clear over persisted runtime memory.
- Sisyphus v2 Phase 6 Task 1 adds pure report/status/trace helpers tolerant of malformed memory arrays.
- Sisyphus report helpers surface verificationSummary, include memory-level evidence, handle awaiting_user conservatively, and sort traces by timestamp.
- Sisyphus v2 Phase 6 Task 2 adds CLI command group for status, trace, report, and confirmed clear with memory backup.

### Important Notes
- JCE plugin model list must use `opencode --pure models`; plain `opencode models` can recurse/load plugins and timeout.
- Audit 2026-05-05: update/plugin hardening covers many prior gaps.


## Archived: 2026-05-06
### Architecture Decisions
- Sisyphus CLI exports testable clear and trace-limit helpers; clear uses deterministic timestamp backup names when given now.
- Sisyphus CLI uses createSisyphusCommand injection to behavior-test commands without process.exit.
- Sisyphus v2 Phase 6 CLI exposes status, trace, report, and clear over persisted execution memory.
- Recovery retry budget is consumed only when an actual retry task is created, not when a retryable failure is recorded.
- Real recovery loop retries failed/invalid delegated tasks through collect-time recovery and blocks with handoff when budget or category prevents retry.
- Recovery retry tasks are launched through spawner after manager creates retry lineage; spawner supports prompt, promptAsync, and chat SDK methods.
- SDK prompt calls use `{ path: { id }, body: { agent, parts: [{ type: "text", text }] } }`; legacy chat fallback keeps `{ params, body.content }`.
- Final review gate evaluates workflow completion, certificate validity, blockers, delegated review acceptance, and exhausted retry history before pass/block.
- Final review gate blocks unresolved structured retry exhaustion via status text, retry flags, or retryCount >= maxRetries; resolved records do not block.
- Final review gate requires retry counts at max to also have an unresolved failure/blocker marker before blocking; delegated review acceptance uses explicit positive formats only.
- Final review gate blocks completion on missing verification, active blockers, unaccepted delegated work, unresolved exhausted retries, or invalid certificate.
- Dispatch prompts now use a formal delegation envelope with goal, scope, non-goals, constraints, allowed files, expected verification, timeout hint, and output contract.
- Plugin completion guard invokes final review gate for completion-like tool output when persisted activeWorkflow exists.
- Blocked final gates append FINAL REVIEW GATE warning before generic verification warning.
- Completed delegated task memory now preserves review status/notes and verification summary so final review gates and CLI reports can reason over accepted/blocked delegated work.
- Final review gate production wiring derives delegated review strings from persisted completed summaries and verification evidence.
- Blocked delegated collect results are converted to recovery blockers instead of being reported as completed success.
- Execution memory merge preserves persisted task history collections by id while refreshing current manager snapshots and workflow runtime.
- Plugin bg_collect persists manager memory immediately so final review gate sees accepted/blocked delegated results without waiting for idle events.
- Accepted retry lineage is marked resolved so successful retries do not trip exhausted retry final-gate checks.
- Plugin runtime agent IDs rebranded to jce-worker and jce-researcher; legacy settings map sisyphus/librarian to new IDs.
- Full JCE-Worker migration removes opencode-jce sisyphus command; legacy files/settings are migration-only inputs.
- Workflow cloneRun deep-copies route skills, evidence entries, and blocker arrays to preserve prior snapshot immutability.
- JCE-Worker status/report expose active workflow routing metadata for operator observability.


## Archived: 2026-05-13
### Architecture Decisions
- Plugin tool.execute.after routes text output into active workflow metadata before final review gate evaluation.
- JCE-Worker runtime routing persists active workflow intent route and uses it for status/report/final-gate policy hints.
- JCE-Worker execution policy evaluates core runtime actions as allow/warn/block decisions in a pure evaluator.
- Dispatch tool evaluates JCE-Worker execution policy before spawning background tasks and surfaces warn/block decisions.
- Route-update policy preserves active review routes against completion claims unless delegated review evidence is accepted.
- JCE-Worker execution policy blocks review-route completion bypass and requires all delegated reviews accepted before final completion.
- JCE-Worker completion policy reuses workflow task-type gates as execution-policy blocks before reporting completion.
- Workflow verification gate requires evidence for review task steps and at least one non-failing evidence item for unknown steps.
- OpenCode install/update/plugin flows now share opencode.json hardening helpers that preserve user config, repair malformed files via backup-and-rebuild, and merge only missing JCE-managed entries.
- JCE-Researcher now uses deep research protocol with modes, source priority, evidence rules, and structured output contract.
- JCE-Researcher upgraded to Research Analyst Pro protocol with query planning, version awareness, evidence ledger, source confidence levels, and conflict handling.
- JCE-Researcher v2.1 adds strategy matrix, evidence budget, source trap rules, decision quality, implementation readiness labels, and red-team self-check.
- JCE workflow tool wrapper exposes read-only summary, verification recipe, safe commit plan, and release readiness actions without plugin registration.
- Plugin entry builds optional Chinese translator from client.session.create/chat and filters non-Write/Edit tool.execute.after string output.
- Blocked final review gate output now continues through later warning and Chinese filtering logic instead of returning early.
- Chinese translator adapter now prefers session.prompt/promptAsync parts requests before chat fallback and reuses spawner response text extraction.

### Important Notes
- Remaining gaps: backup failure handling, malformed opencode.json repair, AGENTS backups, doctor global install prompt, memory backup.
- Audit hardening verified: bun test 321 pass/0 fail; typecheck; bash -n; PowerShell parser all pass.
- Phase 5 skill router uses token matching for single-word markers and precedence: branch, review, completion, parallel, bugfix, feature.
- Update handoff fix: if Bun.which('opencode-jce') fails after shim rewrite, restart via bun run ~/.config/opencode/cli/src/index.ts -- update.
- 2.0.14 includes audit follow-up hardening plus JCE-native Coding Brain v3.1-v3.5.
- gh CLI unauthenticated in this environment; release pages exist for v2.0.12/v2.0.14 but API bodies cannot be edited without auth token.
- Update audit fixed directory merge fetch accounting, CLI staging rollback, and stale context-keeper command path refresh.
- TUI sidebar now appends Token Savings between default sidebar content and LSP via sidebar_content slot, sourced from persisted context budget summary.
- Release bump 2.0.15 prepared with update hardening and TUI Token Savings sidebar telemetry.


## Archived: 2026-05-19
### Architecture Decisions
- v3.0.0 Adaptive AI Orchestration — STATE-OF-THE-ART:
  - Core: TaskGraph, Scheduler, Planner, Agent Protocol, Shared Memory, Evidence v2, Intent Router v2
  - Intelligence: auto-activation, cross-node enrichment, evidence gating, parallel optimization, escalation, wisdom
  - Reliability: error boundaries, plan cancellation, rate limiting, timeouts, approval gate, conflict detection, token budget, rollback, logging, health check
  - Advanced: speculative execution, self-reflection loop, adaptive model selection, memory consolidation, multi-agent consensus
  - 15 modules in src/plugin/lib/orchestration/, 886 tests pass
- JCE-Researcher audit fixes: agent-aware review/evidence scoring, skill injection, model routing, readiness labels, taskType classification
- Android advanced flow pack will extend existing scanner/classifier with profile, diagnostics, verification matrix, and reusable flow templates.
- Android advanced flow pack exports `advanced-flow.ts`: profile, environment findings, failure-aware templates, persistent context hints, and verification matrix.
- Android Phase A-E implemented as modular TypeScript capabilities under `src/plugin/lib/android/` with exported planners/auditors/gates and tests.
- Installers now verify required Android advanced TypeScript payload files in staging before replacing the installed CLI.
- Flutter Advanced Flow Pack added under `src/plugin/lib/flutter/` with scanner, env probe, classifier, verification, flow, command/evidence gates, release readiness.

### Important Notes
- v2.0.15 update initially failed because tag was missing; created and pushed tag v2.0.15 at c48ef49.
- Token Savings sidebar now always renders and reports estimated tokens saved, not percent/char ranges.
- OpenCode v1.14.34 TUI plugins live in tui.json plugin, not opencode.json plugin; tui-only modules must default export {id,tui}.
- v3.3.0 release URL: https://github.com/JCETools-Petra/JCE-Opencode-Tools/releases/tag/v3.3.0
- v3.3.1 patch release URL: https://github.com/JCETools-Petra/JCE-Opencode-Tools/releases/tag/v3.3.1
- v3.3.1 was retagged to ec342d3 for stale OpenCode process cleanup after update/install/reinstall.
- JCE-Worker audit found remaining stop-early gaps: narrow completion regex, TodoWrite history blind spot, passive warnings, stale workflow reset, orchestration fire-and-forget.
- Stop-early fix adds open-work enforcer for TodoWrite pending/in_progress state, Indonesian stop phrases, review-route final gate, and orchestration continuation blocker persistence.
- v3.3.1 retagged to bd3c3d4 after stop-early guard + payload verification; release notes updated.
- JCE audit recommendations: raise skill descriptions/checklists, integrate orchestration dispatch/collect loop, enrich evidence store, and add agent registry QA.
- v3.3.2 release URL: https://github.com/JCETools-Petra/JCE-Opencode-Tools/releases/tag/v3.3.2
- v3.3.2 commit 65b995f fixes greeting/no-task compaction loops and verifies guard payload in install/update paths.


## Archived: 2026-06-02
### Architecture Decisions
- JCE priorities 1-10 baseline adds capability registry, skill audit/resolver, evidence/docs/analytics CLI, JCE doctor checks, and web/api/devops/security flow packs.

### Important Notes
- Context auto-create audit: v3.3.2 still relies on model calling MCP `context_read`; local fix now adds plugin-side first-message template creation but is not committed/released yet.
- v3.3.3 release URL: https://github.com/JCETools-Petra/JCE-Opencode-Tools/releases/tag/v3.3.3
- v3.3.3 commit 3f4bceb bootstraps `.opencode-context.md` on first chat message and restores Fish PATH install support.
- v3.3.4 release URL: https://github.com/JCETools-Petra/JCE-Opencode-Tools/releases/tag/v3.3.4
- v3.3.4 commit ef4abeb explicitly payload-verifies `src/lib/context-template.ts` for install/reinstall/update context bootstrap.
- v3.3.5 release URL: https://github.com/JCETools-Petra/JCE-Opencode-Tools/releases/tag/v3.3.5
- v3.3.5 commit d46ad88 preserves non-empty malformed opencode.json instead of rebuilding defaults during install/update/plugin config merge.
- v3.3.6 commit b250363 and tag v3.3.6 pushed; changelog available on main, GitHub Release page not created due missing gh/token.
- v3.3.6 release URL: https://github.com/JCETools-Petra/JCE-Opencode-Tools/releases/tag/v3.3.6
- v3.4.0 commit 6a85d74 adds native JCE advanced context index and MCP tools for bucketed handoff memory.
- v3.4.0 main commit and tag pushed to origin; release page creation pending.
- v3.4.0 release URL: https://github.com/JCETools-Petra/JCE-Opencode-Tools/releases/tag/v3.4.0
- v3.4.2 commit 6dac5b9 improves context index reliability with async exists, atomic writes, collision-safe filenames, and sanitize/collision tests.
- v3.4.2 main commit and tag pushed to origin; GitHub Release created.


## Archived: 2026-06-05
### Architecture Decisions
- Web/API/DevOps/Security advanced flow packs now include filesystem scanners and `opencode-jce flow <kind>` CLI for local diagnostics.


## Archived: 2026-06-10
### Important Notes
- v3.4.2 release URL: https://github.com/JCETools-Petra/JCE-Opencode-Tools/releases/tag/v3.4.2


## Archived: 2026-06-10
### Important Notes
- v3.5.0 commit 65913a5 adds 11 context index improvements: pruning, stats, search/filter, cache, dedup fix, noise filter, auto-gitignore, configurable descriptions, weighted inference.
- v3.5.0 release URL: https://github.com/JCETools-Petra/JCE-Opencode-Tools/releases/tag/v3.5.0
- Skill audit finding: 74/74 folders mapped and docs-synced, but auto-routing reachability is 69/74; missing auto-route coverage for advanced-patterns, ai-optimization, context-preservation, delegat.
- Suggested post-3.6.0 JCE-Worker roadmap: stronger evidence scoring, resume-safe execution graph, policy gates, failure memory, release diff automation, and deeper Android/runtime verification.
- Update integrity bug: `src/commands/update.ts` uses `git ls-remote refs/tags/vX` tag object SHA for annotated tags; clone HEAD is peeled commit SHA, causing v3.6.0 expected `ffe0bd0` vs actual `1808


## Archived: 2026-06-11
### Important Notes
a4f`.

